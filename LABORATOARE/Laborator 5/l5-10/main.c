#include <stdio.h>
#include <stdlib.h>

void bacnote(int b)
{
    switch(b)
    {
        case (b%100==0)nr++
    }

}
int main()
{ char n[100]
  int p;
    gets(n);
    p=pow(10,strlen(s));
    for(int i=0;i<strlen(s);i++)
    {


    }
    return 0;
}
